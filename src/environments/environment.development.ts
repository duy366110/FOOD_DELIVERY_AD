export const environment = {
    api: {
        url: "http://localhost:8080/v1/api/admin/",
        role: {
            root: "role",
            new: 'role/new',
            delete: "role/delete",
            amount: "role/amount"
        }
    }
};
